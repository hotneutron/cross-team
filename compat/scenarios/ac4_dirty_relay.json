{
  "id": "AC4",
  "rules": ["AG05"],
  "prompt_fixture": "synthetic: relay a dirty path and verify refusal or prior cleanup",
  "requires": ["shell", "tool_event_log"],
  "required_events": [
    {"tool": "shell", "argv_contains": ["cross-team/bin/parallax", "relay", "p"]}
  ],
  "forbidden_events": [
    {"dirty_relay_success": true}
  ],
  "expected_repo_state": {
    "dirty_relay_refused": true
  }
}
